# skill-set

Define, share, and install named, versioned sets of agent skills — an open manifest convention (`<name>.skill-set.json`), a JSON Schema, and a CLI.

> **Status: pre-release.** Under active development. 

CLI published as [`@skill-set/cli`](https://www.npmjs.com/package/@skill-set/cli).

The published [`skills-sets`](https://www.npmjs.com/package/skills-sets) package is a name-reservation stub. 

Docs will live at [skill-set.md](https://skill-set.md).

Not affiliated with the `skills`, `skillset`, `skillsets`, or `skills-set` npm packages.

MIT © Harry Martin
