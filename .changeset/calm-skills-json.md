---
"@skill-set/cli": minor
---

Capture the structured output from `skills add --json`. Install summaries now aggregate passed security checks and attribute each warning or issue to its skill.

Interactive installs show a compact view of the captured upstream output. Press `e` during an install to expand this view.
